<Window x:Class="RegEnforcer.FolderPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Folder" Height="600" Width="400" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="SelectedPathTextBox" Grid.Row="0" Margin="10" IsReadOnly="True"/>
        <ComboBox x:Name="DriveComboBox" Grid.Row="1" Margin="10" SelectionChanged="DriveComboBox_SelectionChanged"/>
        <TreeView x:Name="FolderTreeView" Grid.Row="2" Margin="10" SelectedItemChanged="FolderTreeView_SelectedItemChanged"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="OK" Width="75" Margin="5" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="75" Margin="5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
